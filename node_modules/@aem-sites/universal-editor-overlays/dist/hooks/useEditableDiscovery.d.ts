import { EditableDiscoveryHookParams, EditablesList } from "../types";
declare const useEditableDiscovery: ({ element }?: EditableDiscoveryHookParams) => {
    editables: EditablesList;
};
export { useEditableDiscovery };
